apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: ha-platform
  labels:
    app: postgres
spec:
  clusterIP: None   # Headless service for StatefulSet
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres
